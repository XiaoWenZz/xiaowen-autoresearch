id = "fedft-controller-global-continuity"
kind = "heartbeat"
status = "ACTIVE"
target_thread_id = "019f88d2-2512-75f0-a90d-c4b71e488607"
rrule = "FREQ=MINUTELY;INTERVAL=30"
prompt = """
Use ${CODEX_HOME}/skills/xiaowen-autoresearch/scripts/controller_control_state.py with schema_version=5.
每次 wake 都是该同一 Controller thread 的恢复事务；terminal verify/absorb/route/dispatch/activation/close/finite-block/title/pin/state-CAS 必须在 final 前必须 drain。
Resolve completion_binding and pending_absorption with observe-terminal, verify-pending-terminal, activate-successor, close-objective, or absorb-and-block.
For successor dispatch, include planned minimum revision, exact six-field binding, and either one exact remote-job projection or explicit no-remote-job; after PASS_MODEL_ROUTE the destination must await-successor-activation with that same job expectation before effects, while Controller commits activate-successor immediately without an extra ACK. On PASS, build the terminal from the returned terminal_identity_projection rather than hand-writing identity fields; Executor copies startup_chain_authority including explicit null. A prebound ACTIVE/NONE job is not submission evidence: absent unit plus absent files before late_threshold has zero effect; at threshold wake the same owner to reconcile launch identity. Label every cursor update with source_turn_state=IN_PROGRESS|FINAL; never advance FINAL+NON_TERMINAL for an Executor without exactly one matching ACTIVE/NONE registered remote job, and use one same-owner terminal-recovery wake instead of user rescue.
Callbacks and dispatch receipts are primary wakes; this 30-minute cadence is recovery only, never a workflow-audit trigger. Validate/deduplicate worker issue envelopes and batch nonempty signals to Workflow Evolution task 019fdcaf-75b6-7603-9519-31f49789ee29 using existing thread-list/wait results; no signal means no message or resend.
On an existing worker transition, use read-only workflow_evolution_gate.py in Shadow Mode for forward trace, rule conformance and dispatch-scoped 25M/75M zero-output token bands; every eighth absorbed terminal adds one independent trace sample. Never infer science from activity or create detector state.
不得充当 Explorer/Audit/Executor；不得创建 Goal、Pro sink、外部 watcher；绝不删除或复制 immutable evidence.
"""
