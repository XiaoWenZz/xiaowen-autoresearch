id = "fedft-controller-global-continuity"
kind = "heartbeat"
status = "ACTIVE"
target_thread_id = "019f88d2-2512-75f0-a90d-c4b71e488607"
rrule = "FREQ=MINUTELY;INTERVAL=30"
prompt = """
First run the read-only `${CODEX_HOME}/skills/xiaowen-autoresearch/scripts/workflow_evolution_gate.py controller-context-window`, then `${CODEX_HOME}/skills/xiaowen-autoresearch/scripts/controller_control_state.py show --projection active` with schema_version=5. Do not preload the full Skill, full state, long references, remote/child AGENTS, or Workflow Evolution for this no-effect check; no-effect never routes Workflow Evolution. Treat only callback, terminal/pending absorption or terminal-state, due blocker, missing/failed owner, failed/overdue job, state contradiction, or explicit new-objective admission as actionable. If none is present, return quietly. For an actionable obligation, reload the complete live Skill and only its directly triggered references before effects; use `show --projection full` only for migration, contradiction, rebuild, or replay.
每次 wake 都是同一 Controller thread 的恢复事务；final 前必须 drain terminal verify/absorb/route/dispatch/activation/close/finite-block/title/pin/state-CAS。
Callbacks are compact receipt-only envelopes; open immutable terminal bodies locally before observe/verify/absorb. Resolve completion_binding and pending_absorption with observe-terminal, verify-pending-terminal, activate-successor, close-objective, or absorb-and-block. Terminal absorption, safety, existing-owner continuity, and blocker recovery remain nonblocking.
REQUIRE_ROLLOVER and PAUSE_NEW_OBJECTIVE_ADMISSION block only new-objective admission; context pause never blocks terminal/safety/existing-owner recovery. Complete the runtime-supported compact/rollover before another new objective; this gate creates no state/artifact. Keep callbacks/receipts as primary wakes and the 30-minute cadence recovery-only.
For successor dispatch require one successful receipt, then immediately CAS activate-successor; the destination awaits that minimum revision with the exact binding and remote-job projection or explicit no-remote-job. No final precedes the CAS.
Model route: frozen deterministic work uses named Luna plus durable validator; real carrier uses Sol/xhigh; science/authority uses Sol/max. Preserve source_turn_state=IN_PROGRESS|FINAL and same-owner terminal-recovery wake for FINAL+NON_TERMINAL or interactive auth/credential/approval UI. Validate/deduplicate worker issue envelopes in Shadow Mode; never infer science from activity or create detector state.
不得充当 Explorer/Audit/Executor；不得创建 Goal、Pro sink、外部 watcher；绝不删除或复制 immutable evidence。
"""
