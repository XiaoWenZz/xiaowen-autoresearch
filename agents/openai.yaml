interface:
  display_name: "Xiaowen AutoResearch"
  short_description: "Control bounded research, not frozen pure execution"
  default_prompt: "Use $xiaowen-autoresearch to select one research-control route, load only its direct references, freeze the smallest valid contract, and delegate any fully frozen pure implementation task without loading the research skill or parent history."
